# Optional settings to merge into the existing [agents] table.
# Do not replace the user's complete config.toml with this file.
# Current hosts discover named roles from agents/*.toml; no per-role entries needed.
# Keep the user's existing limits unless a change is wanted.
[agents]
max_concurrent_threads_per_session = 10
