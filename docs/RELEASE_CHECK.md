# 2026-09-15 发布检查

## 发布范围修订

初次发布后的用户确认将范围收敛到 12 个 Skill，移除四个专项目录。本机安装未修改。剩余 12 个 Skill 重新进行格式检查，并检查更新后的文件引用与 Git 差异。以下初次发布检查保留为当时记录。

## 范围与保留

本次将维护环境当前的全局规则、10 个角色和 16 个自定义 Skill 同步到公开仓库。复制阶段对角色与 Skill 文件进行 SHA-256 比较，未发现不一致。随后只进行 [发布记录](HISTORY.md) 中列出的路径、隐私和许可调整。没有用仓库旧版反向覆盖本机生效提示词。

全局工作循环与委派边界保留在 `global/AGENTS.md`。各角色完整指令保留在其 TOML。任务方法与触发条件保留在各 Skill 中。旧装配材料移出当前安装入口，历史提交保留。中英文 README 的开场对话与结尾保留，技术说明按现行结构更新。

## 文字审查

按提示词编写方法核对变化范围、规则所在层、保留内容和新增指导的依据。新安装说明明确角色目录发现与显式配置引用的区别，也区分文件安装、宿主发现和实际行为。没有新增强制委派、固定计划数量或整套覆盖安装要求。

对新增说明检查否定、枚举、抽象措辞与长句。权限保护和避免覆盖用户配置属于具体边界，保留明确说明。角色与 Skill 清单属于本次有界发布内容，保留完整枚举。现有提示词正文不借打包机会整体重写。

## 检查项目

- 10 份角色 TOML 均可解析，必需字段存在。
- 16 个 Skill 均通过 `quick_validate.py`。
- 检查 YAML、JSON、Markdown 本地链接及指定的本机路径 / 凭据模式。
- 排除备份、缓存和个人联系图片，保留第三方许可证。
- 提交前检查 Git 差异与远端变化，不强制覆盖远端。

以上是发布内容检查。尚未在全新接收环境运行安装和真实任务，不能据此宣称跨宿主兼容或模型行为已经验证。
