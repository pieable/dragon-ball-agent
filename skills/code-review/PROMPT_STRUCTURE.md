# 代码审查 Skill

来源：同目录 `SKILL.md`。

## 2026-09-13 修订对应

下文的原文行号保留为本次修订前的来源定位。当前正文的变化如下，其余规则去向沿用下文说明。

区分恢复路径缺少决定性证据与已有证据证明路径不必要或错误。前者补证后仍未知时给出证据不足，后者要求修改。两者均不能无依据通过交付。保留每次单列兜底结果、合同要求的保护与恢复能力、三态结论、只读范围和独立评审授权。将末节的证据、角色、权限和问题处理分段。

## 骨架正文

评审判断当前候选能否在约定范围内安全合入或交付。结论只覆盖该候选与合同，不替代用户整个任务的完成判断，也不重新决定产品目标。

先确认基线、候选、差异、应产生的结果、已接受约束与已有证据。沿会改变结论的项目规则、实现和调用关系恢复合同。缺少决定性材料时补足或给出证据不足，不能猜测作者意图。

先判断需求、设计与系统集成是否成立，再根据真实后果深入局部风险。检查遗漏、相反行为和未经要求的范围增加。审查深度与变更风险相称，只读取判断所需的周边范围。

先判断正确性、必需行为、数据保护、权限和生命周期是否受到影响，再考虑维护与风格。风险由真实变更产生，具体检查随证据调整，不按固定类别凑齐意见。检查新增复杂度是否有当前需求或可靠证据支持，既有保护是否仍然成立。

独立核验证据是否对应当前候选，以及测试能否捕获目标错误。要求和真实接口决定预期，作者自述不替代证据。涉及真实宿主、界面、外部依赖或模型行为的声明，需要相应运行证据。

依据事实、实际影响和证据充分程度给出“可合入”“要求修改”或“证据不足”。没有发现问题不等于证据充分。只报告候选引入或暴露、作者可纠正且影响本次交付的问题，区分阻塞问题与非阻塞建议。

报告先给结论和范围，再按严重程度说明发现。每项发现给出准确位置、触发条件或证据、影响及需要纠正的机制。即使无阻塞问题，也说明验证依据、限制和未验证路径。候选变化使结论可能失效时，只复审受影响范围。

每次评审单列兜底路径审查结果，无发现时也明确说明。技术事实、项目规则和可复现证据高于个人偏好。纯风格意见、无实际影响的推测和无关旧问题不阻塞交付，可靠质量门禁已完全覆盖的机械问题不重复报告。

只读审查不修改代码或提供补丁。授权开发中的自查可以在原范围内修复并验证，但不能称为独立评审。阻塞发现必须经修复、技术证据反驳或用户明确改变要求处理，不得静默略过。

## 功能块及来源

| 功能块 | 原文位置 | 细化责任与内容 |
| --- | --- | --- |
| C1 兜底路径专项审查 | 27–36；44 的单列要求 | 对主路径之外继续产生结果的路径，依次核查必要性、已有机制、触发/语义/来源/验证，以及隐藏失败、重复副作用和竞争路径。保留明确阻塞标准及每次单列结果。 |
| C2 异步与生命周期 | 52 | 创建、发布、取消、报错、清理，以及竞态、所有权和销毁后行为。 |
| C3 接口、配置与权限 | 53 | 提供方与全部调用方、默认值依据、拒绝路径的全部执行入口。 |
| C4 状态、缓存与派生视图 | 54 | 权威来源、更新、通知、读取一致性及失败状态。 |
| C5 模型行为协议 | 55 | prompt、schema、transcript 的实际模型可见内容，按需 snapshot 或真实链路核验。 |
| C6 加载与外部集成 | 56 | 发布入口、导出、注册、配置和失败诊断。 |
| C7 最终输出边界 | 57 | 长度、字节数、包装层、批量和异常输入，检查最终发出或保存的结果。 |
| C8 项目可接续资料 | 58 | 维护所需稳定信息的可发现性与真实性。 |
| C9 文档与可见文字 | 59 | 行为说明同步，以及产品文字是否帮助使用者，设计解释与成品语义的落实。 |
| C10 实现范围与复杂度 | 60 | 复用既有机制，检查无需求的抽象、配置、兜底或额外重构。 |
| C11 当前协作装配 | 66 | Astra 自查与用户授权后启用 code-reviewer，独立角色与合同限定权限。 |

## 原文覆盖

8–25 是评审目标、对象、主干与风险比例，全部由骨架承担。27–36 是 C1。40–48 是结论和报告骨架，其中单列兜底的部分归 C1。50–60 是 C2–C10。62 是专项风险按需深入的调用原则。64–68 是权限与证据处理骨架，其中具体模型和角色调用归 C11。frontmatter 是触发元数据。

C1 细化兜底判断的方法，但每次单列结果已经是用户明确指定的评审职责，直接写入本 Skill 骨架。它不依赖 GPT 特点，不能因为与 GPT 专属审查同为专项就认定归属相同。C2–C10 在变更触及相应机制且会改变结论时深入，其基础风险判断由骨架承载。

## 历史核对：2026-09-13

比较 `../../prompt-lab/backups/before-related-skill-optimization-20260826/code-review/SKILL.md`、本目录 `MODIFICATION_LOG.md`、`../code-development/MODIFICATION_LOG.md` 与 `../../agents/PROMPT_HISTORY.md`。

- 8 月 26 日前版本第 8–18、31–35 行已明确从后果形成风险面、事实高于偏好、按风险控制范围和不重复可靠门禁。本次补足这些判断，未把整张专项风险表提升为每次必查清单。
- 8 月 29 日角色历史把评审从问题清单明确为三态交付判断。9 月 9 日改为支持 Astra 自查时，三态结论、只读审查、证据边界与阻塞处理继续保留。本稿维持这些骨架，具体模型与启用方式留在 C11。
- Agent 历史的 9 月 2 日确认及开发日志 9 月 8 日再次确认“每次单列兜底”。本次将固定报告职责补入骨架，专项路径的检查展开留在 C1。

本次只修正分类说明，不修改 Skill 或 reviewer 运行正文。

辅助理由见 `../../prompt-lab/docs/subagent-architecture/rule-rationale.md` R33、R35、R38，以及 `../../prompt-lab/docs/changes/engineering-loop-20260908.md` 20–22、48–52。它们解释了三态结论为何存在、普通复杂度检查为何不足以阻止竞争兜底，以及为何不能把“Skill 已读取”当作评审处理完成。本次保留固定职责，但未把当年的 Qt、数据库实例或模型推理档位写进通用骨架。
