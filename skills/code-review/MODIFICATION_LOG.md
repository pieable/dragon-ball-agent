# Code Review Skill 修改记录

## 2026-09-13 三个 Skill 审查后的局部修订

用户授权按本次审查报告修改并按 write-instructions-zh 再审。区分恢复路径缺少决定性证据与已有证据证明路径不必要或错误。前者补证后仍未知时给出证据不足，后者要求修改。两者均不能无依据通过交付。保留每次单列兜底结果、合同要求的保护与恢复能力、三态结论、只读范围和独立评审授权。将末节的证据、角色、权限和问题处理分段。

修改前正文与本记录备份在本次任务 work/three-skills-edit/before。逐项核对修改范围、规则去向及完整差异，完成通用审查和 GPT 专属文字审查。未运行新的真实开发、评审或多代理研究任务，文字检查不证明实际行为收益。

这份文件不参与 Skill 运行。它保存会改变评审判断的规则来路。

## 2026-09-09 明确评审判断顺序和与 reviewer 的分工

用户要求优化正文，并说明最初参考 DeepSeek Harness GitHub 上的 code review Skill、与 reviewer agent 关系紧密。本次核对当前本地正文、角色指令和修改记录，并阅读当前上游 https://github.com/deepseek-ai/deepseek-harness/blob/master/.agents/skills/dsh-code-review/SKILL.md 。当前上游用于对照方法，未核实最初复制时的提交，不作为覆盖后来本地规则的依据。

本次变化：从 reviewer 已有职责明确需求、设计与集成判断，再按实际后果深入检查；按 code-development 已有标准补足测试能否抓住目标错误、证据对应当前候选的判断；将报告要求集中到结论部分，补齐 reviewer 已有的问题定位、证据、非阻塞建议和未验证路径要求；明确 Skill 提供评审标准，独立角色和合同限定其权限及交回责任。其余正文保持不变。

保留兜底专项审查及每次单列、三种结论、真实路径证据、只读边界、授权后启用独立子代理和阻塞问题处理。code-reviewer 角色允许取不到 Skill 时按角色正文继续，因此保留其独立执行所需的核心标准；本次没有修改角色、内联 Worker Base、模型或工具配置，也没有导入 DeepSeek 仓库专用命令和检查。

修改前正文、记录和角色文件已备份到本次任务 work 目录。完成实际差异、规则对应关系和角色文件未变核对。验证限于文字和文件，没有启动 reviewer 子代理或证明真实宿主已加载、评审行为已改善。

## 2026-09-09 支持 Astra 自查和用户授权的独立评审

用户决定默认由 Astra 直接工作，并要求 description 允许提议启用子代理评审。本次去掉上下级组织规则，区分实现者自查和独立评审。只要求审查的任务仍只读，已授权开发任务中的自查问题由 Astra 在原范围内修正。独立子代理评审在用户授权后使用。证据标准、兜底专项审查、风险判断和阻塞问题处理保持有效。

修改前副本保存在 `.codex/backups/astra-direct-skills-20260908-235700/`。本次只验证文字、元数据和新 CLI 提示词装配，未创建评审子代理作行为验证。

## 2026-08-25 增加实现范围和复杂度审查

Base 和 `code-development` 已经分别承载共同工作边界和代码实现细则。评审者只需要判断交付候选是否引入了没有支撑当前结果的维护成本，因此本次只增加一个审查方向，没有复制实现者的操作指令。

评审发现仍然需要说明实际代码、触发条件和影响。评审者不提供补丁，也不替实现负责人选择产品方向或实现路线。

文字已经静态复审。真实评审任务中的行为效果尚未验证。
