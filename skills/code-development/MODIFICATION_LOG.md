# Code Development Skill 修改记录

## 2026-09-13 三个 Skill 审查后的局部修订

用户授权按本次审查报告修改并按 write-instructions-zh 再审。明确停止扩张针对核心结果持续失败或无效补偿，避免把普通修复误判成换路条件。按责任分开入口核实、复用、约束和删除失效路径的段落。实验归因按相关因素检查，把“第一项错误转换”明确为首次偏离预期的步骤。真实入口、首次正常操作、最终候选证据、局部修改、低复杂度、必要重复和评审触发范围继续保留。

修改前正文与本记录备份在本次任务 work/three-skills-edit/before。逐项核对修改范围、规则去向及完整差异，完成通用审查和 GPT 专属文字审查。未运行新的真实开发、评审或多代理研究任务，文字检查不证明实际行为收益。

## 2026-09-09 整理正文中的触发条件和验证时点

用户在正文审查后同意按四点作局部修改：评审方法交由 code-review 承载；真实路径验证按开始确认入口、首次可运行操作和交付证据组织；最小结果用于验证当前关键判断，产品方向未定时再取得核心价值反馈；需求变化时更新已有且会误导实现的资料，新增资料仍按交付条件判断。同时将“唯一实现”改为依据一致要求实现，明确限制没有需求或证据支持的替代路径。

保留实际入口与版本核实、正常用户操作、真实依赖验收、兜底约束、独立子代理授权及停止验证条件。未涉及的正文保持不变。修改前正文和本记录已备份到本次任务 work 目录。完成正文差异与规则去向核对；没有进行真实开发任务的行为验证。

## 2026-09-09 Astra 直接开发，独立评审按用户授权启用

用户决定日常工作由 Astra 直接完成，删除上下级、下属合同和评审交接流程。真实入口、正常用户操作、实现范围、证据边界和问题修正要求保留。Astra 按首次使用者的视角实际操作候选，自查不称为独立评审。需要独立视角时可以提议启用 `code-reviewer`，获得用户授权后再使用。

正文和默认调用提示同步。修改前副本保存在 `.codex/backups/astra-direct-skills-20260908-235700/`。本次核验文字差异、元数据和新 CLI 提示词装配，不以静态检查证明后续开发质量。

## 2026-09-08 补足需求更新与正常入口验收

用户在十轮游戏开发测试后授权先弥补可改善的执行缺口，并要求避免整套恢复造成负担。对照 2026-09-05 归档与当前正文，真实入口、原因判断、独立评审和证据边界已有要求，本次不重复恢复整套章节。

仅新增两段：需求变化时同步受影响的实现依据、项目说明与下属合同；正常流程首次可运行时取得代表性实际操作证据，同时核实验证执行条件，并保持证据对应交付候选。code-executor 单独明确等待必要验证并处理结果的阶段责任。没有新增固定评审轮次、必填文档或统计测试要求。

修改前四份文件备份于 `.codex/backups/engineering-gaps-20260908-brief`。本次仅做文字与配置差异检查，未再次运行模型能力对照或行为测试，效果留待后续实际任务观察。

## 2026-09-08 连续实现与成果验收

用户根据实际代码任务授权调整流程：减少同一使用路径上的反复交接，把实现、实际运行和修正交给一个负责人，研究与评审按会改变结果的未知和风险触发。Astra 程序更新遗漏日常 EXE、直播测试遗漏核心行为、RP 测试器投入过重是本次调查的具体依据。

将重复的路线、Demo、试验和停止判断合并到连续实现过程，保留核心结果沿真实使用路径成立的标准。统计、并发、随机性、合法终态和归因细节迁入按需参考。最低复杂度、局部修改、兜底必要性、权限、独立评审与首次用户视角验收继续有效。开发角色读取本 Skill，减少角色全文复述。证据已经支持完成标准后结束，新增验证须针对会改变交付判断的具体疑点。

用户再次明确兜底审查必须每次单列，包括没有发现问题的情况；此要求保留，code-review Skill 原文未作语义修改。角色接替判断写在 Root，并在阶段角色保留交接责任。

备份、逐项规则去向与实际样本验证见 `.codex/prompt-lab/engineering-loop-20260908.md`。历史任务与当前版本不同，样本结果不证明长期收益。

这份文件不参与 Skill 运行。它保存规则为什么存在，帮助以后修改时保留已经验证有价值的行为。

## 2026-08-25 恢复最低复杂度的代码细则

用户在重写 Base 时确认，跨任务的共同原则只需要要求复用现有能力、选择满足当前结果的最低复杂度，并保持局部修改的范围。代码实现中的具体做法由本 Skill 承载。

本次恢复的语义来自用户提供的网上流出的 Claude Code 提示词摘录。来源没有在本次修改中独立核实，因此只作为来路说明。用户已经确认这些规则符合自己的长期代码工作经验。

运行正文现在要求一次性操作直接实现，几行相似代码优先于过早抽象。内部代码和框架已有明确保证时直接依赖，把校验放在真实系统边界。局部修复只覆盖当前结果，不顺手扩成周边清理、额外配置或未来设计。确认失效的代码直接删除，不留下没有实际用途的兼容痕迹。

这些规则保留当前合同明确要求的兼容性、安全、数据保护、信任边界和可访问性。它们约束过度工程化，不削弱真实边界或用户要求。

文字已经静态复审。真实代码任务中的行为效果尚未验证。

## 2026-08-27 调整阶段内回报和角色路由

两个新 Base 任务中，Root 虽然把执行交给 subagent，仍直接创建和管理大量调查、实现、评审、浏览器与验证责任，并通过密集的 followup、send 和 wait 跟进。Sol、Terra 和 Luna 的模型回合因此处于相近数量级。问题不是角色分工缺失，而是阶段内结果持续回到 Root，阶段负责人没有完整吸收下属报告、返工和验收。

本次保留简单责任直接交给执行角色、复杂模块交给 `code-executor` 的分工。阶段预计需要多个下属或多轮调整时，由阶段负责人直接管理并向上级交回一份阶段结果。实现已有阶段负责人时，评审、返工和重新验证也回到该负责人。执行角色的具体选择改由当前注册的角色说明提供，本 Skill 不再复制本地取证、网络取证、浏览器和 Worker 的角色表。

同一责任继续、创建新责任和等待的固定动作已经由全局 `AGENTS.md` 直接绑定到 collaboration 工具，角色提示词不再重复轮询和等待过程。文字、TOML 和公开副本完成静态验证。新建 `worker-luna` 已确认实际加载共同执行层。Root 调用是否下降仍需在后续真实任务中统计。
