# 代码开发 Skill

来源：同目录 `SKILL.md` 及 `references/experiments.md`。

## 2026-09-13 修订对应

下文的原文行号保留为本次修订前的来源定位。当前正文的变化如下，其余规则去向沿用下文说明。

明确停止扩张针对核心结果持续失败或无效补偿，避免把普通修复误判成换路条件。按责任分开入口核实、复用、约束和删除失效路径的段落。实验归因按相关因素检查，把“第一项错误转换”明确为首次偏离预期的步骤。真实入口、首次正常操作、最终候选证据、局部修改、低复杂度、必要重复和评审触发范围继续保留。

## 骨架正文

把用户要求落实为可以实际使用、并有相应证据的代码结果。由同一实现责任人连续掌握使用路径的调查、判断、修改、运行和修正，按反馈推进。

从用户要获得的结果、修改边界和可观察验收路径开始。沿真实入口、调用关系、状态和依赖取得足以解释当前行为的证据。核实当前实现与使用版本，目标行为和接入方式清楚后进入实现。只有会改变重要接口、架构或用户结果的未知需要先行设计或实验。

开始修改前确认必须保留的行为、相关测试和已有未提交改动，保护用户已有工作。从与目标有关的入口逐步追踪调用与数据关系，先形成足以解释行为的系统理解，再深入决定实现的局部，不以通读仓库代替有方向的调查。

成品形态、核心价值或完成标准尚有实质分歧时，说明选择及影响，由用户决定。普通实现选择在已有授权内持续完成。需求变化后先修正会误导后续工作的权威说明。

先取得能够验证关键判断的最小完整结果，使代表性输入经过真实入口、核心处理、必要状态与依赖产生输出。每次变更包含同一行为所需的实现、验证和说明，增加接口时接通真实调用者，及时运行并修正。

核心结果失败时，先区分局部缺陷与路线问题。当前方法持续增加补偿却不能推进目标时，重新判断是否继续、缩小或换路。改变已约定的形态、要求或明显投入时，将依据和影响交给用户。

实现复杂度服从当前已证实需求。优先复用项目和平台的可靠能力，依据权威要求完成必要修改，保留合同要求的安全、数据保护、兼容和可访问性。未经需求或证据支持，不扩大实现和维护范围。

一次性操作直接实现，几行相似代码不必提前抽象。内部和框架已经提供可靠保证时直接使用，校验放在真实系统边界。局部修复保持局部，确认失效的代码删除，不留下没有实际用途的兼容痕迹。

用能够证明目标行为、也能捕获目标错误的证据判断完成。核验当前交付候选，按修改后果选择测试、集成或真实用户流程。测试依据来自要求和实际接口。声明真实宿主、模型或完整用户流程可用时，取得对应路径证据。

首次正常流程可运行时完成一次代表性实际操作，发现入口和集成问题后及时修正。测试失败先查清原因，不能扩大修改范围来掩盖失败。多部分修改整合后取得必要的跨模块证据，交付前确认这些证据对应当前候选。

按风险进行实现者复核和评审，处理实际发现与决定性证据缺口。自查与独立评审分别表述。全部完成标准已有证据支持，且剩余未知不会改变结论时结束验证。

交付约定入口下可使用的产物，说明结果、关键变化、验证和限制。未来维护依赖无法从代码与测试恢复的信息时，更新已有权威资料。保持当前成品、使用说明和维护依据一致。

## 功能块及来源

| 功能块 | 原文位置 | 细化哪项骨架责任 | 保留条件 |
| --- | --- | --- | --- |
| D1 Astra 连续执行与独立评审授权 | 正文 8、50–52 | 实现责任连续、评审方式 | 当前协作策略；Astra 名称和 code-reviewer 授权约定单列，不能带进所有模型的骨架。 |
| D2 日常入口与隔离候选 | 12、46、48、58 | 真实使用路径与交付证据 | 存在版本、构建、宿主或加载差异时，明确用户日常入口与候选入口，保留首次正常操作及最终候选证据核对。 |
| D3 平台行为核验 | 14 | 用事实决定实现 | 依赖当前平台或组件行为时，查当前源码或官方资料，必要时验证加载。 |
| D4 受控实验与结果归因 | 30；experiments.md 1–11 | 最小实验与可靠验证 | 并发、性能、概率、不可逆状态或受限服务需要设计观测时。包括归因单位、真实版本、合法终态、计划内重复及停止条件。 |
| D5 兜底路径的必要性与失败语义 | 36、38 | 权威要求与最低必要复杂度 | 主路径失败后改用其他来源仍产生结果时，核对需求依据、触发条件、语义和验证，保留必要恢复能力。 |
| D6 局部代码改动校准 | 34、40 | 控制实现范围 | 直接实现、边界校验、局部修复和删除失效代码已在骨架表达。helper、配置、注释的具体例子只用于校准，不替代基本行动。 |
| D7 可见界面首次用户验证 | 46 | 验证真实用户流程 | 交付涉及界面、视觉反馈和完整操作时。 |
| D8 项目可接续资料 | 20、60 | 当前依据一致与可维护交付 | 稳定信息无法从代码和测试恢复时，更新既有资料，缺入口时用 AGENTS 索引，并标明否决路线条件。 |
| D9 分阶段发布 | 58 | 控制外部影响 | 存在发布扩展时，先有限范围反馈再扩大，按授权执行。 |

## 原文覆盖

8 为总体骨架与 D1 的混合。12–20 为入口、设计、需求及决策骨架，附 D2、D3、D8。24–28 为最小结果和路线判断骨架。30 路由 D4。34–40 为范围骨架及 D5、D6。44–54 为验证、评审与停止骨架，附 D1、D2、D7。58–62 为交付骨架，附 D8、D9。frontmatter 描述保留为发现与触发元数据。

本 Skill 中多数直接行动属于骨架。实验文件内“先明确要回答的问题”同样是骨架原则在特定实验条件下的重述；将文件整体列作功能块不意味着其基本原则可以从整体设计消失。

## 历史核对：2026-09-13

比较 `../../prompt-lab/backups/code-development.SKILL.before-long-wait-20260802.md`、本目录 `MODIFICATION_LOG.md` 及当前正文。

- 8 月 2 日前版本第 8–12、18、24–30 行已经要求持续掌握调用链、保留已有改动、定向调查、共享状态不并行修改、集成测试、查清失败和证据充分后停止。本次补足分类稿遗漏的动作。保护已有改动现在由共同执行层承担，不宣称当前 Skill 独立承载全部规则。
- 8 月 25 日记录明确一次性直接实现、边界校验、局部修改和清除失效代码来自用户确认的长期代码经验。本次把这些基础行动写入骨架，D6 只保留具体校准。
- 9 月 8–9 日记录连续保留真实入口、首次正常操作、最终候选证据及必要保护。本次把验证时点补入骨架，D2 继续细化有多版本和宿主差异时的核对。
- 9 月 9 日用户决定 Astra 直接开发，旧版强制委派与上下级流程已经被替换，未从历史恢复。具体实验归因仍留在 D4，独立评审授权维持 D1。

历史用于证明规则语义和变更来路，不证明当前模型已经按这些要求工作。

辅助理由继续核对 `../../prompt-lab/docs/changes/custom-skill-restoration-2026-08-14.md` 49–53、`../../prompt-lab/docs/changes/engineering-loop-20260908.md` 及 `../../prompt-lab/docs/subagent-architecture/rule-rationale.md` R10、R31、R36、R38。它们分别说明：保护已有工作与查清失败是恢复过的基础边界；连续责任避免反复交接；兜底不能用于对冲对合同的不确定；文档只补足代码和测试无法恢复的稳定信息。当前开发 Skill 已用更合适的要求表述取代旧“唯一实现”措辞，本稿未恢复该旧措辞。
